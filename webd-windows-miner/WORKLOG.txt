WebDollar Windows Miner - Work Log

Date: 2026-04-02

Checkpoint 001
- Scope fixed: Windows app first, Android resumes after desktop MVP validates wallet, pool and mining flow.
- Stack fixed: Electron + TypeScript + Vue.
- Project created at d:\Webdollar_2026\webd-windows-miner.
- Implemented: Electron main process, preload bridge, Vue renderer, local config persistence, pool stats HTTP service, initial desktop dashboard.
- Build verified successfully with npm run build.
- Config currently persisted locally: poolUrl, walletAddress, poolKey, threadCount, autoStart.
- Current UI can save local config and fetch public pool stats from the HTTP backend.
- Next step approved: implement wallet compatibility layer for legacy .webd import/export and encrypted local wallet storage, then continue with auth/get-job/share/mining loop.

Rule for future checkpoints
- Save progress in this file at major milestones.
- Commit and push the Windows app regularly so progress can be reviewed remotely.

Checkpoint 002
- Ported wallet compatibility layer from Android into Electron backend.
- Implemented wallet generate, raw import, legacy .webd export, WIF/private-key import compatibility.
- Implemented encrypted local wallet storage using AES-256-GCM + PBKDF2-SHA256.
- Added wallet UI in the desktop dashboard for generate/import/export/encrypt/unlock flows.
- Build verified successfully again with npm run build.
- Next step in progress: worker auth, get-job and share-submit client flow for the desktop miner.

Checkpoint 003
- Implemented desktop worker API client for auth, get-job, worker-stats and share-submit flows.
- Extended the dashboard with protocol diagnostics for token, workerId, current job, worker stats and manual share submit.
- Build verified successfully again with npm run build.
- Next step in progress: replace manual share submit with the real desktop mining loop and live hashrate/stats updates.

Checkpoint 004
- Added automated desktop mining loop (start/stop) using auth -> get job -> hash range -> submit share cycle.
- Added CPU hash engine service (double SHA-256 placeholder parity with Android MVP logic) and live hashrate meter.
- Added runtime mining counters in UI: accepted/rejected/stale shares, status and last result.
- Build verified successfully again with npm run build.
- Next step: harden miner loop with retries/backoff tuning and then run practical test session before switching back to Android fixes.

Checkpoint 005
- Fixed Electron dev startup crash caused by tsup bundling of electron internals (`Dynamic require of fs`).
- Updated tsup scripts to externalize electron in both watch and build modes.
- Added resilient dev URL retry logic in Electron main process to avoid transient Vite race (`ERR_CONNECTION_REFUSED`).
- Verified `npm run dev` starts cleanly (only non-blocking DevTools Autofill warnings remain).

Checkpoint 006
- Fixed renderer hydration crash when desktop bridge is unavailable (`Cannot read properties of undefined (reading loadConfig)`).
- Added resilient desktopApi fallback layer: config/meta now work with safe localStorage fallback when `window.desktopApi` is missing.
- Wallet-only operations now return explicit Electron-runtime errors instead of hard crashing.
- Build verified successfully again with npm run build.

Checkpoint 007
- Set SpyClub legacy pool address as the default pool across Electron config and renderer fallback.
- Added pool address normalization so `pool/.../https:$$host:port` is auto-resolved to usable API base URL format.
- Added startup auto-connect sequence: on app load, if wallet exists, run worker auth + job + stats automatically; if autoStart is enabled, mining loop starts automatically.
- Build verified successfully again with npm run build.

Checkpoint 008
- Addressed logs reported from runtime: preload script module mismatch and legacy pool REST mismatch symptoms.
- Switched Electron bundle format to CJS for main/preload compatibility and removed `import.meta` dependency from main process path resolution.
- Added fallback API candidate logic for legacy `pool/...` addresses with clear error messaging when REST `/worker/*` is not available remotely.
- Guarded tray startup to avoid dev crash when tray icon is missing.
- Verified `npm run dev` startup no longer crashes (only non-blocking DevTools Autofill warnings remain).

Checkpoint 009
- Fixed preload path duplication issue (`dist-electron/dist-electron/preload.js`) by resolving preload relative to Electron main runtime directory.
- Kept packaged renderer path resolution compatible with current dist layout.
- Reordered legacy pool API candidates to try local REST backend first, reducing noisy 404 from remote legacy host.
- Verified build and dev startup after changes.

Checkpoint 010
- Implemented direct Legacy Socket.IO bridge in Electron (parse pool address, hello-pool, get-work, new-work, work-done).
- Wired legacy IPC methods into preload and renderer services, so `pool/...` addresses now use legacy flow directly instead of depending on local REST backend.
- Kept REST path support for non-legacy URLs and fallback behavior for mixed environments.
- Build verified successfully after integrating legacy mode.

Checkpoint 011
- Addressed legacy connection instability (`xhr poll error`) by moving Socket.IO client to legacy-compatible major (2.x) and tuning options for legacy pools.
- Set websocket-first transport order and disabled strict TLS rejection for legacy endpoints.
- Added TypeScript shim for legacy socket client package to keep strict build green.
- Build verified successfully after dependency and bridge updates.

Checkpoint 012
- Aligned Windows legacy client with the Android compatibility baseline by switching Socket.IO client to version 1.0.1.
- Restored polling-first transport order for legacy connection attempts.
- Improved `connect_error` reporting to include message, description, type and status when available.
- Build verified successfully after the legacy downgrade.

Checkpoint 013
- Simplified the desktop mining UI to a single `Start mining` button and a single `Stop mining` button.
- Removed manual auth, job-fetch and manual share-submit controls from the main mining flow.
- Kept worker/session diagnostics visible while leaving authentication and job handling automatic inside the mining loop.
- Build verified successfully after the UI cleanup.

Checkpoint 014
- Stabilized direct legacy pool connectivity through multiple protocol and runtime fixes in Electron and legacy Socket.IO bridge.
- Fixed legacy hello payload for PoS pools by sending unencoded miner address list, removing server-side undefined length failures.
- Fixed duplicate activity-log keys and reduced repeated log spam in UI.
- Confirmed current desktop app can mine in legacy mode.

Checkpoint 015 (Baseline)
- First working mining baseline locked as the reference version for future development.
- Decision recorded: from now on, all new work starts from this working baseline.
- All changes up to this point saved and pushed.

Checkpoint 016
- Switched UI/config behavior to PoS-only single worker mode: thread count is fixed to 1 in renderer and Electron config persistence.
- Replaced editable Threads field with explicit PoS single-worker mode indicator.
- Added wallet value section in mining diagnostics (pending, confirmed, total WEBD).
- Legacy bridge now stores reward/confirmed values from hello-pool answer and exposes them via worker stats.
- Build verified successfully after these improvements.

Checkpoint 017 (Plan + Checklist 1/2/3/5)
- Scope approved for next baseline iteration:
	1) Earnings history view
	2) ETA payout estimate
	3) No-job watchdog alert
	5) Miner-only simple mode

Implementation plan
- Step 1: Extend config schema with `simpleMode` and `payoutTarget` and persist in renderer + Electron.
- Step 2: Add miner-only mode toggle and hide advanced sections when enabled.
- Step 3: Add periodic worker-stats snapshots and render mini earnings chart.
- Step 4: Add payout ETA estimate based on reward growth history and target.
- Step 5: Add watchdog timer for missing jobs and user-facing warning banner.
- Step 6: Build verification + checkpoint save/push.

Checklist salvari
- [x] Config schema updated (`simpleMode`, `payoutTarget`) in types, renderer fallback and Electron config.
- [x] Miner-only mode toggle implemented and persisted.
- [x] Earnings history snapshots implemented and chart added in UI.
- [x] ETA payout implemented from reward growth and configurable target.
- [x] No-job watchdog implemented with warning banner and activity log signal.
- [x] Build verified successfully after full implementation.

Checkpoint 018
- Fixed mining loop reliability issue where mining could stop after worker stats refresh or transient share/session errors.
- Converted per-iteration errors into auto-recovery flow (`Recovering...`) instead of stopping the whole miner.
- On transient errors, session/job is reset and miner retries auth/job automatically, without needing manual Start button again.
- Build verified successfully after resilience update.