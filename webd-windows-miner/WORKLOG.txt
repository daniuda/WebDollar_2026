WebDollar Windows Miner - Work Log

Date: 2026-04-02

Checkpoint 001
- Scope fixed: Windows app first, Android resumes after desktop MVP validates wallet, pool and mining flow.
- Stack fixed: Electron + TypeScript + Vue.
- Project created at d:\Webdollar_2026\webd-windows-miner.
- Implemented: Electron main process, preload bridge, Vue renderer, local config persistence, pool stats HTTP service, initial desktop dashboard.
- Build verified successfully with npm run build.
- Config currently persisted locally: poolUrl, walletAddress, poolKey, threadCount, autoStart.
- Current UI can save local config and fetch public pool stats from the HTTP backend.
- Next step approved: implement wallet compatibility layer for legacy .webd import/export and encrypted local wallet storage, then continue with auth/get-job/share/mining loop.

Rule for future checkpoints
- Save progress in this file at major milestones.
- Commit and push the Windows app regularly so progress can be reviewed remotely.

Checkpoint 002
- Ported wallet compatibility layer from Android into Electron backend.
- Implemented wallet generate, raw import, legacy .webd export, WIF/private-key import compatibility.
- Implemented encrypted local wallet storage using AES-256-GCM + PBKDF2-SHA256.
- Added wallet UI in the desktop dashboard for generate/import/export/encrypt/unlock flows.
- Build verified successfully again with npm run build.
- Next step in progress: worker auth, get-job and share-submit client flow for the desktop miner.

Checkpoint 003
- Implemented desktop worker API client for auth, get-job, worker-stats and share-submit flows.
- Extended the dashboard with protocol diagnostics for token, workerId, current job, worker stats and manual share submit.
- Build verified successfully again with npm run build.
- Next step in progress: replace manual share submit with the real desktop mining loop and live hashrate/stats updates.

Checkpoint 004
- Added automated desktop mining loop (start/stop) using auth -> get job -> hash range -> submit share cycle.
- Added CPU hash engine service (double SHA-256 placeholder parity with Android MVP logic) and live hashrate meter.
- Added runtime mining counters in UI: accepted/rejected/stale shares, status and last result.
- Build verified successfully again with npm run build.
- Next step: harden miner loop with retries/backoff tuning and then run practical test session before switching back to Android fixes.

Checkpoint 005
- Fixed Electron dev startup crash caused by tsup bundling of electron internals (`Dynamic require of fs`).
- Updated tsup scripts to externalize electron in both watch and build modes.
- Added resilient dev URL retry logic in Electron main process to avoid transient Vite race (`ERR_CONNECTION_REFUSED`).
- Verified `npm run dev` starts cleanly (only non-blocking DevTools Autofill warnings remain).

Checkpoint 006
- Fixed renderer hydration crash when desktop bridge is unavailable (`Cannot read properties of undefined (reading loadConfig)`).
- Added resilient desktopApi fallback layer: config/meta now work with safe localStorage fallback when `window.desktopApi` is missing.
- Wallet-only operations now return explicit Electron-runtime errors instead of hard crashing.
- Build verified successfully again with npm run build.

Checkpoint 007
- Set SpyClub legacy pool address as the default pool across Electron config and renderer fallback.
- Added pool address normalization so `pool/.../https:$$host:port` is auto-resolved to usable API base URL format.
- Added startup auto-connect sequence: on app load, if wallet exists, run worker auth + job + stats automatically; if autoStart is enabled, mining loop starts automatically.
- Build verified successfully again with npm run build.

Checkpoint 008
- Addressed logs reported from runtime: preload script module mismatch and legacy pool REST mismatch symptoms.
- Switched Electron bundle format to CJS for main/preload compatibility and removed `import.meta` dependency from main process path resolution.
- Added fallback API candidate logic for legacy `pool/...` addresses with clear error messaging when REST `/worker/*` is not available remotely.
- Guarded tray startup to avoid dev crash when tray icon is missing.
- Verified `npm run dev` startup no longer crashes (only non-blocking DevTools Autofill warnings remain).

Checkpoint 009
- Fixed preload path duplication issue (`dist-electron/dist-electron/preload.js`) by resolving preload relative to Electron main runtime directory.
- Kept packaged renderer path resolution compatible with current dist layout.
- Reordered legacy pool API candidates to try local REST backend first, reducing noisy 404 from remote legacy host.
- Verified build and dev startup after changes.

Checkpoint 010
- Implemented direct Legacy Socket.IO bridge in Electron (parse pool address, hello-pool, get-work, new-work, work-done).
- Wired legacy IPC methods into preload and renderer services, so `pool/...` addresses now use legacy flow directly instead of depending on local REST backend.
- Kept REST path support for non-legacy URLs and fallback behavior for mixed environments.
- Build verified successfully after integrating legacy mode.

Checkpoint 011
- Addressed legacy connection instability (`xhr poll error`) by moving Socket.IO client to legacy-compatible major (2.x) and tuning options for legacy pools.
- Set websocket-first transport order and disabled strict TLS rejection for legacy endpoints.
- Added TypeScript shim for legacy socket client package to keep strict build green.
- Build verified successfully after dependency and bridge updates.

Checkpoint 012
- Aligned Windows legacy client with the Android compatibility baseline by switching Socket.IO client to version 1.0.1.
- Restored polling-first transport order for legacy connection attempts.
- Improved `connect_error` reporting to include message, description, type and status when available.
- Build verified successfully after the legacy downgrade.

Checkpoint 013
- Simplified the desktop mining UI to a single `Start mining` button and a single `Stop mining` button.
- Removed manual auth, job-fetch and manual share-submit controls from the main mining flow.
- Kept worker/session diagnostics visible while leaving authentication and job handling automatic inside the mining loop.
- Build verified successfully after the UI cleanup.

Checkpoint 014
- Stabilized direct legacy pool connectivity through multiple protocol and runtime fixes in Electron and legacy Socket.IO bridge.
- Fixed legacy hello payload for PoS pools by sending unencoded miner address list, removing server-side undefined length failures.
- Fixed duplicate activity-log keys and reduced repeated log spam in UI.
- Confirmed current desktop app can mine in legacy mode.

Checkpoint 015 (Baseline)
- First working mining baseline locked as the reference version for future development.
- Decision recorded: from now on, all new work starts from this working baseline.
- All changes up to this point saved and pushed.

Checkpoint 016
- Switched UI/config behavior to PoS-only single worker mode: thread count is fixed to 1 in renderer and Electron config persistence.
- Replaced editable Threads field with explicit PoS single-worker mode indicator.
- Added wallet value section in mining diagnostics (pending, confirmed, total WEBD).
- Legacy bridge now stores reward/confirmed values from hello-pool answer and exposes them via worker stats.
- Build verified successfully after these improvements.

Checkpoint 017 (Plan + Checklist 1/2/3/5)
- Scope approved for next baseline iteration:
	1) Earnings history view
	2) ETA payout estimate
	3) No-job watchdog alert
	5) Miner-only simple mode

Implementation plan
- Step 1: Extend config schema with `simpleMode` and `payoutTarget` and persist in renderer + Electron.
- Step 2: Add miner-only mode toggle and hide advanced sections when enabled.
- Step 3: Add periodic worker-stats snapshots and render mini earnings chart.
- Step 4: Add payout ETA estimate based on reward growth history and target.
- Step 5: Add watchdog timer for missing jobs and user-facing warning banner.
- Step 6: Build verification + checkpoint save/push.

Checklist salvari
- [x] Config schema updated (`simpleMode`, `payoutTarget`) in types, renderer fallback and Electron config.
- [x] Miner-only mode toggle implemented and persisted.
- [x] Earnings history snapshots implemented and chart added in UI.
- [x] ETA payout implemented from reward growth and configurable target.
- [x] No-job watchdog implemented with warning banner and activity log signal.
- [x] Build verified successfully after full implementation.

Checkpoint 018
- Fixed mining loop reliability issue where mining could stop after worker stats refresh or transient share/session errors.
- Converted per-iteration errors into auto-recovery flow (`Recovering...`) instead of stopping the whole miner.
- On transient errors, session/job is reset and miner retries auth/job automatically, without needing manual Start button again.
- Build verified successfully after resilience update.

Checkpoint 019
- Implemented direct wallet import from local `.webd` file picker in Electron (Open File dialog + `.webd` filter).
- Added new IPC bridge method (`wallet:select-file-raw`) exposed to renderer via preload and desktopApi wrapper.
- Updated wallet UI: replaced raw-text import workflow with explicit `Import .webd file` action.
- Kept legacy parser compatibility in backend, so selected file content is validated by existing wallet import logic.
- Build verified successfully after the file-import flow update.

Checkpoint 020
- Fixed legacy PoS auth IPC issue (`An object could not be cloned`) by sending a plain wallet object instead of Vue reactive proxy.
- Disabled legacy auto-connect on startup when only wallet address exists but wallet is not unlocked/imported in runtime.
- Forced fresh legacy auth session at Start mining so submit-share is always bound to the currently unlocked wallet.
- Build verified successfully after the PoS auth/session fixes.

Checkpoint 021
- Improved wallet/payout visibility in UI when pool public miner endpoints are missing or inconsistent.
- Added robust pool reward fetch logic with endpoint fallback (`/pools/all-miners` and `/pools/miners`) and flexible payload parsing.
- Added UI fallback for payout cards to show worker-session values instead of `-` when pool payout list is unavailable.
- Build verified successfully after wallet value display hardening.
Checkpoint 022
- Updated wallet value cards to prioritize historical pool totals, not just current mining-session stats.
- Wallet total now reflects cumulative value up to now using pool totals (pending + sent).
- Clarified UI labels to indicate historical pool source for wallet values.
- Build verified successfully after historical value display update.

Checkpoint 023 (Conversation Summary + New Development Base)
- Confirmed live pool miner match for address `WEBD/gD+dt3gD4/OLR37m6yozeN0emz+Pj+qK2H/` with exact values:
	- `totalPOSBalance = 109934908` (units) => `10,993.4908 WEBD`
	- `reward_total = 15324`, `reward_confirmed = 5696`, `reward_sent = 0`
- Fixed pool address canonicalization mismatch in app by decoding legacy pool symbols before normalization:
	- `# -> O`, `$ -> /`, `@ -> l`
	- This aligns app matching with validated PowerShell canonicalization and unblocks correct miner lookup.
- Wallet total display now uses `totalPOSBalance` as primary source when present, with correct WEBD unit conversion.
- Project baseline decision recorded by user: this current stable state is the new base for continued development.
- Saved as git commit `c1b8ba5` and tagged `v1-base` to replace previous "first version" reference.

Checkpoint 024
- Saved a separate project document: `POOL_REGISTRY_ARCHITECTURE.txt`.
- Document contains the logical architecture for an open pool registry where anyone can submit a pool and the site verifies it automatically before listing.
- Recorded permanent resume rule: if user says `reluam aplicatia de minat`, continue from the desktop miner baseline, latest WORKLOG checkpoint and current git state.
- Recorded permanent collaboration rule: for each new project, ask the user which exact resume words should be used later to continue that specific project.

Checkpoint 025
- Saved a future-reference document for `pool_registry`: `POOL_REGISTRY_REFERENCE_vue-Frontend.txt`.
- Source documented: `https://github.com/WebDollar/vue-Frontend`.
- Saved a practical summary of the repo purpose, tech stack, build commands and how it can help as inspiration for a future public WebDollar website or pool registry.
- Explicitly recorded that this external repo is reference material only; the active main project remains the desktop mining app.

Checkpoint 026
- Saved `POOL_REGISTRY_INTEGRATION_PLAN_vue-Frontend.txt`.
- Document describes how a future pool registry could be split into modules if `vue-Frontend` is used only as inspiration/basis for the public UI layer.
- Explicitly separated responsibilities between frontend pages, API client/store, verification backend, scheduled worker, storage and admin layer.
- Reconfirmed in the saved plan that the active main project remains the desktop mining app and that `reluam aplicatia de minat` must resume from the miner state.

Checkpoint 027
- Saved `MINING_PAGE_UX_FLOW_with_POOL_REGISTRY.txt`.
- Document reframes the future public site correctly: main product is a mining page, while the pool registry remains a secondary support module for choosing verified pools.
- Saved the proposed main user flows: Home/Start Mining, Choose Pool, Pool Details, Submit Pool, Help/Setup.
- Saved the UX rule that the site must push users toward wallet + pool selection + mining start, not toward an admin-style registry as the primary experience.

Checkpoint 028
- Updated `MINING_PAGE_UX_FLOW_with_POOL_REGISTRY.txt` with the new mining-first rule: no primary Start button in the ideal flow.
- Saved the new expected behavior: if the wallet is already imported/unlocked and the user meets the PoS minimum, mining should start automatically when the page opens.
- Saved the pool-selection rule: choose automatically from the current `verified` / `recommended` pool list, using random selection among healthy online pools.
- Documented the origin of the `100 WEBD` minimum as a PoS/core protocol rule from `Node-WebDollar`, not as a UI-only rule.

Checkpoint 029
- Resumed mining app work and generated a standalone runnable Windows build output.
- Fixed Electron packaging version format in `package.json` from `0.001` to valid semver `0.0.1`.
- Verified full app build steps pass (`typecheck`, renderer build, electron main/preload build).
- Standalone executable confirmed at `dist/win-unpacked/WebDollar Windows Miner.exe`.
- NSIS installer generation is currently blocked in this environment by Windows symlink privilege error while extracting `winCodeSign` cache; unpacked executable build remains usable for independent run/testing.

Checkpoint 030
- Simplified the main UI header while keeping essential mining controls visible.
- Removed the top toggle for simple/advanced mode from hero actions.
- Kept core controls in primary view: wallet operations, pool config, save config, refresh pool, start/stop mining, wallet value cards.
- Moved technical/noisy areas behind `Detalii tehnice` by showing build marker and pool diagnostics panel only when advanced details are enabled.
- Build verified successfully after UI adjustments.

Checkpoint 031
- Removed the left sidebar info blocks from the main UI (Desktop prototype, Runtime, Status MVP), as requested.
- Kept main mining controls and content area as primary focus.
- Disabled the default Electron application menu (`Menu.setApplicationMenu(null)`), which removes the Help menu from the window.
- Build verified successfully after UI and Electron menu cleanup.

Checkpoint 032 (UI trim pass v0.0.2)
- Updated app version to `0.0.2` as a new iteration marker.
- Implemented requested UI removals in wallet/mining diagnostics area:
	- removed `Public key`, `Secret status`, `Legacy .webd export preview`
	- removed texts `Android-compatible format bridge` and `Importul se face direct din fisier ...`
	- removed `Payout target`, `ETA payout`, and `Earnings trend` blocks
- Wallet address display is now normalized with `$` separators for UI readability.
- Added wallet total (`Wallet total portofel (balance pool)`) next to wallet address in the wallet panel.
- Hashrate display now shows minimum `1 H/s` while mining is running, even if sampled value is 0.
- Adjusted visual density (smaller paddings/fonts, compact card grids) for a tighter layout.
- Build verified successfully after full changes.

Checkpoint 033 (UI cleanup + anti-flicker v0.0.3)
- Updated app version to `0.0.3` for a separate safe iteration.
- Removed `Pool key` and `Mining mode` fields from the visible config UI.
- Kept essential controls requested by user: wallet import/generate/encrypt/unlock, pool URL, save config, start/stop, wallet values.
- Implemented anti-flicker behavior during mining by making worker auth/job/stats refreshes silent in the mining loop (`runWorkerAuth/loadWorkerJob/loadWorkerStats` with `silent` mode).
- Result: no repetitive success-banner churn while mining; idle behavior remains unchanged.
- Build verified successfully after changes.

Checkpoint 034 (Beta prep: cleanup + installer + docs)
- Removed beta-internal marker text from UI (build marker card and message for protocol-log/pool-host-fix are no longer visible in executable build).
- Replaced top header placeholder texts (`Phase 1 running`, `Desktop control surface`, and follow-up line) with `WEBDOLLAR Windows Miner`.
- Hid temporary diagnostics block from main view to avoid exposing noisy `Pool Legacy poll`/pool visibility rows during beta.
- Normalized `Local config -> Wallet address` input for UI readability: displays `$` but persists `/` internally for protocol compatibility.
- Verified build successfully (`npm run build`).
- Generated Windows NSIS installer successfully (`npm run dist`).
- Installer artifact: `dist/WebDollar Windows Miner Setup 0.0.3.exe`.
- Standalone artifact: `dist/win-unpacked/WebDollar Windows Miner.exe`.
- Added beta release checklist and user guide documents for publishing and support handoff.
- Saved discussion snapshot for session continuity.

