server {
    listen 80;
    server_name checkout.domeniu.ro;

    # Redirectare HTTPS (recomandat după ce ai certificat SSL)
    # return 301 https://$host$request_uri;

    location /api/ {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        root /var/www/webd-checkout;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}

# Versiune HTTPS (după certbot --nginx)
# server {
#     listen 443 ssl;
#     server_name checkout.domeniu.ro;
#     ssl_certificate     /etc/letsencrypt/live/checkout.domeniu.ro/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/checkout.domeniu.ro/privkey.pem;
#
#     location /api/ {
#         proxy_pass http://localhost:3002;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#     location / {
#         root /var/www/webd-checkout;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
# }
